SRC = Glob("../common/*/*.c", strings=True) + Glob("../common/*.c")
#print (SOURCE_LIST)

#env.Program('hello', 'hello.cpp')
COMMON_PATH = "../common"

Export('COMMON_PATH')

SConscript('atmega/SConscript')